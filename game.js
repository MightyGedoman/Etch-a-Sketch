// for now grid will be the user input variable.
let grid = 0






function createGrid() {
    while(currentgrid<grid)[
        //creat element functon part.
    ]
}


function checkInput() {
    if(grid<16){
        console.log('please enter a value larger than 16')
    }
}